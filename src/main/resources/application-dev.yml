server:
  port: 8090

spring:
  application:
    name: user-microservice
  datasource:
    url: jdbc:mysql://localhost:3306/users_microservice
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"