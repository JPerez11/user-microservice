server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost/users_microservice
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"